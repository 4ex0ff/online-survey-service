FROM nginx:1.27-alpine

# Runtime-only image for weak VPS hosts. The React app must already be built
# into frontend/dist by CI or a developer machine before this image is built.
COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY dist /usr/share/nginx/html

EXPOSE 80
